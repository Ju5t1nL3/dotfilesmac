set -g @catppuccin_status_background "#1e1e2e"
set -g @catppuccin_window_text " #W"
set -g @catppuccin_window_current_text " #W#{?window_zoomed_flag,(),}"
set -g @catppuccin_window_status_style "rounded"
set -g @catppuccin_status_left_separator  " "
set -g @catppuccin_status_right_separator " "
set -g @catppuccin_status_right_separator_inverse "no"
set -g @catppuccin_status_connect_separator "no"
set -g status-right-length 100
run ~/.config/tmux/plugins/catppuccin/tmux/catppuccin.tmux

# tmux 3.7 draws prompts as an overlay unless the message style has `fill`.
# Keep command prompts on one complete, left-aligned status line.
set -gF message-style "fg=#{@thm_teal},bg=#{E:@catppuccin_status_background},align=left,fill=#{E:@catppuccin_status_background}"
set -gF message-command-style "fg=#{@thm_teal},bg=#{E:@catppuccin_status_background},align=left,fill=#{E:@catppuccin_status_background}"

# Make the status line pretty and add some modules
set -g status-left "#{E:@catppuccin_status_session}"
set -g status-right "#{E:@catppuccin_status_application}"
set -ag status-right "#{E:@catppuccin_status_directory}"
set -ag status-right "#{E:@catppuccin_status_uptime}"
